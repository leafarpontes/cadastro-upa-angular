import { PacienteAtendidoDirective } from './paciente-atendido.directive';

describe('PacienteAtendidoDirective', () => {
  it('should create an instance', () => {
    const directive = new PacienteAtendidoDirective();
    expect(directive).toBeTruthy();
  });
});
